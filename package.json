{
  "name": "eclipse-library",
  "version": "1.0.0",
  "description": "A static digital library for reading short stories, hosted on GitHub Pages.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/validate-stories.js",
    "preview": "npx http-server . -p 8000 -c-1"
  },
  "devDependencies": {
    "marked": "^14.0.0"
  }
}
